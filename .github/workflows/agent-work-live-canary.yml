name: Agent Work Live Canary

# Live rollout gate targets the PR #215 immutable release gateway. The gateway
# wraps the isolated Controller V3 core with the canonical Agent Work SSE adapter,
# so the authenticated browser test exercises the same stream contract production uses.
on:
  workflow_dispatch:
  push:
    branches:
      - fix/agent-work-meaningful-runtime-v2
    paths:
      - .github/workflows/agent-work-live-canary.yml
      - e2e/agent-work-live-canary.spec.ts
      - src/components/AssistantWorkIndicator.tsx
      - src/components/__tests__/AssistantWorkIndicatorLifecycle.test.ts
      - src/services/agentActivityReducer.ts
      - src/services/agentWorkCompletionBarrier.ts
      - src/services/sseParser.ts
      - src/services/messageRepository.ts
      - src/services/__tests__/agentWorkCompletionBarrier.test.ts
      - src/services/__tests__/agentControllerV2RuntimeBoundary.test.ts
      - src/services/__tests__/geminiTerminalSynthesisContinuation.test.ts
      - src/store/useMessageStore.ts
      - src/store/__tests__/useMessageStoreAgentWorkPersistence.test.ts
      - supabase/functions/openai-assistant-core-v2/index.ts
      - supabase/functions/openai-assistant-core-v3/index.ts
      - supabase/functions/openai-assistant-core-v3/import_map.json
      - supabase/functions/openai-assistant-v2-entry-router/index.ts
      - supabase/functions/_shared/runtime/runtimeFlags.ts
      - supabase/functions/_shared/agentWorkDurableStream.ts
      - supabase/functions/_shared/agentWorkSseAdapter.ts
      - supabase/functions/_shared/safeStreamSink.ts
      - supabase/functions/_shared/geminiInteractionsTransportGA.ts

permissions:
  contents: read

concurrency:
  group: agent-work-live-canary-${{ github.ref }}
  cancel-in-progress: true

jobs:
  meaningful-work:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      CI: 'true'
      VITE_SUPABASE_URL: https://bpbbvjigostgrssnduhk.supabase.co
      VITE_SUPABASE_ANON_KEY: sb_publishable_dzZaPjNDRK22UUwLXAiS4g_JerlvhHQ
      VITE_REASONING_ENGINE_V2: 'true'
      VITE_SINGLE_ASSISTANT_RUNTIME: 'true'
      E2E_USERNAME: ${{ secrets.E2E_USERNAME }}
      E2E_PASSWORD: ${{ secrets.E2E_PASSWORD }}
      E2E_ASSISTANT_CANARY_SLUG: agent-work-release-215-gateway
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Require authenticated browser credentials
        shell: bash
        run: |
          set -euo pipefail
          test -n "${E2E_USERNAME}" || { echo 'E2E_USERNAME secret is required'; exit 1; }
          test -n "${E2E_PASSWORD}" || { echo 'E2E_PASSWORD secret is required'; exit 1; }

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.9.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Run authenticated Agent Work canary
        run: pnpm test:e2e e2e/agent-work-live-canary.spec.ts

      - name: Upload Playwright diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: agent-work-live-canary-${{ github.sha }}
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 3
